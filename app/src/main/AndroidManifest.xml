<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="micromodules.app.main">
    <application android:label="@string/app_name"
                 android:icon="@mipmap/ic_launcher"
                 android:roundIcon="@mipmap/ic_launcher_round"
                 android:supportsRtl="false" tools:ignore="GoogleAppIndexingWarning">

        <provider
                android:name=".ApplicationBootstrap"
                android:authorities="${applicationId}.bootstrap"
                android:exported="false"/>
    </application>
</manifest>
